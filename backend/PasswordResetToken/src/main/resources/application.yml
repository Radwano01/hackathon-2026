server:
  port: 8070

spring:
  application:
    name: PasswordResetToken

  datasource:
    url: jdbc:postgresql://localhost:5432/password_reset_tokens
    username: radwan
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: www.radwaniq@gmail.com
    password: pewdbhhgxsysodrr
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

